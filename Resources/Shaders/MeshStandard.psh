struct Input
{
	float4 position : SV_POSITION;
	float3 normal : NORMAL;
	float2 texCoord : TEXCOORD0;
};

struct Output
{
	float4 color : SV_TARGET;
};

Output main(Input input)
{
	Output output = (Output)0;
	
	output.color = float4(input.normal * 0.5f + 0.5f.xxx, 1.0f);
	
	return output;
}